sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t monsieuregermain/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true monsieuregermain/react-test npm test

after_success:
  - docker build -t monsieuregermain/multi-client ./client
  - docker build -t monsieuregermain/multi-nginx ./nginx
  - docker build -t monsieuregermain/multi-server ./server
  - docker build -t monsieuregermain/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push monsieuregermain/multi-client
  - docker push monsieuregermain/multi-nginx
  - docker push monsieuregermain/multi-server
  - docker push monsieuregermain/multi-worker
